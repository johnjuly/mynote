#

* stackoverflow: 函数堆积在栈里面 it overflows the amount of memory you have,too many recursive functions called
tell the algorithm to stop doing self-calling at some point
so if else :base case(easy one) ;recursive case
