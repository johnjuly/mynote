#

3.13


## chaperter 2

* trick about 记忆hex
记住三个对应的二进制a c f 10 12 14

* 当x =2^n,轻松转换成十六进制
  * x的二进制表示是1后面跟着n个0；
  * 十六进制数字0表示4个二进制的0，故n=i+4j;j表示0的个数，0<=i<=3;
  * 例子：x=2048=2^11,n=11=3+4*2；0x800;

* 字长：指明**指针**数据的nominal size,子长决定的最重要的参数是虚拟地址空间的大小
* 32位程序与64位程序的不同之处在于改程序是如何编译的，而不是其运行的机器类型

* 字节顺序 大小端之争 格列夫游记 敲鸡蛋🥚
* 位级运算的常见用法：
  **掩码**运算 掩码 从一个字中选出位的集合，由x的最低有效字节组成的值x&0xFF;
  
